import React from "react";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import {
  faSearch,
  faShoppingBag,
  faUser,
  faSmile,
  faFilter,
  faShoppingCart,
  faBars,
  faTshirt,
  faWindowClose,
  faCheck,
  faCaretLeft,
  faCaretDown,
  faCaretUp,
  faCommentDots,
  faMobileAlt,
} from "@fortawesome/free-solid-svg-icons";

import {
  faInstagram,
  faPinterest,
  faTwitterSquare,
  faFacebook,
} from "@fortawesome/free-brands-svg-icons";

const searchIconLM: JSX.Element = <FontAwesomeIcon icon={faSearch} />;
const bagIconLM: JSX.Element = <FontAwesomeIcon icon={faShoppingBag} />;
const userIconLM: JSX.Element = <FontAwesomeIcon icon={faUser} />;
const smileIconLM: JSX.Element = <FontAwesomeIcon icon={faSmile} />;
const filterIconLM: JSX.Element = <FontAwesomeIcon icon={faFilter} />;
const cartIconLM: JSX.Element = <FontAwesomeIcon icon={faShoppingCart} />;
const menuIconLM: JSX.Element = <FontAwesomeIcon icon={faBars} />;
const shirtIconLM: JSX.Element = <FontAwesomeIcon icon={faTshirt} />;
const checkIconLM: JSX.Element = <FontAwesomeIcon icon={faCheck} />;
const closeIconLM: JSX.Element = <FontAwesomeIcon icon={faWindowClose} />;
const arrowLeftIconLM: JSX.Element = <FontAwesomeIcon icon={faCaretLeft} />;
const arrowDownIconLM: JSX.Element = <FontAwesomeIcon icon={faCaretDown} />;
const arrowUpIconLM: JSX.Element = <FontAwesomeIcon icon={faCaretUp} />;
const commentIconLM: JSX.Element = <FontAwesomeIcon icon={faCommentDots} />;
const phoneIconLM: JSX.Element = <FontAwesomeIcon icon={faMobileAlt} />;

const instagramIconLM: JSX.Element = <FontAwesomeIcon icon={faInstagram} />;
const pinterestIconLM: JSX.Element = <FontAwesomeIcon icon={faPinterest} />;
const twitterIconLM: JSX.Element = <FontAwesomeIcon icon={faTwitterSquare} />;
const facebookIconLM: JSX.Element = <FontAwesomeIcon icon={faFacebook} />;

export {
  searchIconLM,
  bagIconLM,
  userIconLM,
  smileIconLM,
  filterIconLM,
  cartIconLM,
  menuIconLM,
  shirtIconLM,
  checkIconLM,
  closeIconLM,
  arrowLeftIconLM,
  arrowDownIconLM,
  arrowUpIconLM,
  commentIconLM,
  phoneIconLM,
  
  instagramIconLM,
  pinterestIconLM,
  twitterIconLM,
  facebookIconLM,
};
